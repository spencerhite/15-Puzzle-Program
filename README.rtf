Directions nohow to run the fifteen puzzle problem correctly:

Insert your corresponding 16 numbers into the `initial` list with 0 being the the blank space. 

Your line of code should look similar to this:

Next, you can run the program. You will be running the breadth first search algorithm and then the depth first search algorithm.

You will see the moves taken to find the solution, the number of nodes expanded, the run time, and the peak memory usage in the output display.

Spencer Hite
